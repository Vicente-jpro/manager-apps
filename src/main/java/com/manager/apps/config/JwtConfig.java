package com.manager.apps.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
    // JWT configuration is handled in SecurityConfig and JwtService
}
